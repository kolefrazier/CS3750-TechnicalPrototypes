@model IEnumerable<CS3750TechnicalPrototypes.Models.Sponsor>

@{
    ViewData["Title"] = "Index";
}

<h2>Sponsors</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.sponsorName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.sponsorEmail)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sponsorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sponsorEmail)
            </td>
            <td>
                <div class="btn-group">
                    <a asp-action="Edit" asp-route-id="@item.sponsorID" class="btn btn-primary btn-sm">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.sponsorID" class="btn btn-primary btn-sm">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.sponsorID" class="btn btn-danger btn-sm">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
