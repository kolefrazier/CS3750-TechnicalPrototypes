@model IEnumerable<CS3750TechnicalPrototypes.Models.MediaType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MediaDescription)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MediaDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MediaTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MediaTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MediaTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
